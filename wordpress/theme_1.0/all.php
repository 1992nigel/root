
	
	<div class="flat flex margin_0">
		
		<div class="margin_0">

				<div class="split">
				<?php

				$args = array(
				    'posts_per_page' => 1, 
				    //'cat' => 'ID OF THE CATEGORY',
				);
				$q = new WP_Query( $args);

				if ( $q->have_posts() ) {
				    while ( $q->have_posts() ) {
				    $q->the_post();        

						get_template_part( 'components/components-post-title-thumbnail' );

				    }
				    wp_reset_postdata();
				}

				?>
			</div>

			<div class="split">
				<?php

				$args = array(
				    'posts_per_page' => 1, 
				    //'cat' => 'ID OF THE CATEGORY',
				);
				$q = new WP_Query( $args);

				if ( $q->have_posts() ) {
				    while ( $q->have_posts() ) {
				    $q->the_post();        

						get_template_part( 'components/components-post-title-thumbnail' );

				    }
				    wp_reset_postdata();
				}

				?>
			</div>

			<div class="split">
				<?php

				$args = array(
				    'posts_per_page' => 1, 
				    //'cat' => 'ID OF THE CATEGORY',
				);
				$q = new WP_Query( $args);

				if ( $q->have_posts() ) {
				    while ( $q->have_posts() ) {
				    $q->the_post();        

						get_template_part( 'components/components-post-title-thumbnail' );

				    }
				    wp_reset_postdata();
				}

				?>
			</div>
		</div>

			
		<div class="margin_0">

			<div class="flat">

				<div class="left">
				<?php

				$args = array(
				    'posts_per_page' => 1, 
				    //'cat' => 'ID OF THE CATEGORY',
				);
				$q = new WP_Query( $args);

				if ( $q->have_posts() ) {
				    while ( $q->have_posts() ) {
				    $q->the_post();        

						get_template_part( 'components/components-post-title-thumbnail' );

				    }
				    wp_reset_postdata();
				}

				?>
			</div>

			<div class="left">
				<?php

				$args = array(
				    'posts_per_page' => 1, 
				    //'cat' => 'ID OF THE CATEGORY',
				);
				$q = new WP_Query( $args);

				if ( $q->have_posts() ) {
				    while ( $q->have_posts() ) {
				    $q->the_post();        

						get_template_part( 'components/components-post-title-thumbnail' );

				    }
				    wp_reset_postdata();
				}

				?>
			</div>

			</div>



				<div class="flat">

					<div class="left">
					<?php

					$args = array(
					    'posts_per_page' => 1, 
					    //'cat' => 'ID OF THE CATEGORY',
					);
					$q = new WP_Query( $args);

					if ( $q->have_posts() ) {
					    while ( $q->have_posts() ) {
					    $q->the_post();        

							get_template_part( 'components/components-post-title-thumbnail' );

					    }
					    wp_reset_postdata();
					}

					?>
				</div>

				<div class="left">
					<?php

					$args = array(
					    'posts_per_page' => 1, 
					    //'cat' => 'ID OF THE CATEGORY',
					);
					$q = new WP_Query( $args);

					if ( $q->have_posts() ) {
					    while ( $q->have_posts() ) {
					    $q->the_post();        

							get_template_part( 'components/components-post-title-thumbnail' );

					    }
					    wp_reset_postdata();
					}

					?>
				</div>

			</div>

		</div>

	</div>

	<div class="flat flex margin_0">
						
		<div class="margin_0">

			<div class="flat">

				<div class="left">
				<?php

				$args = array(
				    'posts_per_page' => 1, 
				    //'cat' => 'ID OF THE CATEGORY',
				);
				$q = new WP_Query( $args);

				if ( $q->have_posts() ) {
				    while ( $q->have_posts() ) {
				    $q->the_post();        

						get_template_part( 'components/components-post-title-thumbnail' );

				    }
				    wp_reset_postdata();
				}

				?>
			</div>

			<div class="left">
				<?php

				$args = array(
				    'posts_per_page' => 1, 
				    //'cat' => 'ID OF THE CATEGORY',
				);
				$q = new WP_Query( $args);

				if ( $q->have_posts() ) {
				    while ( $q->have_posts() ) {
				    $q->the_post();        

						get_template_part( 'components/components-post-title-thumbnail' );

				    }
				    wp_reset_postdata();
				}

				?>
			</div>

			</div>



				<div class="flat">

					<div class="left">
					<?php

					$args = array(
					    'posts_per_page' => 1, 
					    //'cat' => 'ID OF THE CATEGORY',
					);
					$q = new WP_Query( $args);

					if ( $q->have_posts() ) {
					    while ( $q->have_posts() ) {
					    $q->the_post();        

							get_template_part( 'components/components-post-title-thumbnail' );

					    }
					    wp_reset_postdata();
					}

					?>
				</div>

				<div class="left">
					<?php

					$args = array(
					    'posts_per_page' => 1, 
					    //'cat' => 'ID OF THE CATEGORY',
					);
					$q = new WP_Query( $args);

					if ( $q->have_posts() ) {
					    while ( $q->have_posts() ) {
					    $q->the_post();        

							get_template_part( 'components/components-post-title-thumbnail' );

					    }
					    wp_reset_postdata();
					}

					?>
				</div>

			</div>

		</div>

	</div>

	<div class="flat flex margin_0">
		
		<div class="margin_0">

				<div class="split">
				<?php

				$args = array(
				    'posts_per_page' => 1, 
				    //'cat' => 'ID OF THE CATEGORY',
				);
				$q = new WP_Query( $args);

				if ( $q->have_posts() ) {
				    while ( $q->have_posts() ) {
				    $q->the_post();        

						get_template_part( 'components/components-post-title-thumbnail' );

				    }
				    wp_reset_postdata();
				}

				?>
			</div>

			<div class="split">
				<?php

				$args = array(
				    'posts_per_page' => 1, 
				    //'cat' => 'ID OF THE CATEGORY',
				);
				$q = new WP_Query( $args);

				if ( $q->have_posts() ) {
				    while ( $q->have_posts() ) {
				    $q->the_post();        

						get_template_part( 'components/components-post-title-thumbnail' );

				    }
				    wp_reset_postdata();
				}

				?>
			</div>

			<div class="split">
				<?php

				$args = array(
				    'posts_per_page' => 1, 
				    //'cat' => 'ID OF THE CATEGORY',
				);
				$q = new WP_Query( $args);

				if ( $q->have_posts() ) {
				    while ( $q->have_posts() ) {
				    $q->the_post();        

						get_template_part( 'components/components-post-title-thumbnail' );

				    }
				    wp_reset_postdata();
				}

				?>
			</div>
		</div>

	</div>